import { RepTagDirective } from './rep-tag.directive';

describe('RepTagDirective', () => {
  it('should create an instance', () => {
    const directive = new RepTagDirective();
    expect(directive).toBeTruthy();
  });
});
